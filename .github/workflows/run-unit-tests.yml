name: Unit Tests

on:
  pull_request:
    types: [opened]

jobs:
  unit:
    runs-on: ubuntu-latest
    name: 🚀 Lint and test
    timeout-minutes: 5
    steps:
      - name: 📥 Install dependencies
        run: npm install --force

      - name: 💅 clean
        run: npm run clean

      - name: 📘 compile
        run: npm run compile

      - name: ✅ Run tests
        run: npm run test
